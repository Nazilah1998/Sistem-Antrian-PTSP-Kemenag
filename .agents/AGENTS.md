# AGENTS.md - Project Workspace Rules

## Strict Command Restrictions
- **DILARANG SEGARIS PUN / NEVER RUN `npm run dev` & `npm run build`**:
  AI Agent TIDAK BOLEH secara otomatis mengeksekusi atau menjalankan perintah `npm run dev` maupun `npm run build` sendiri. Biarkan USER yang mengeksekusinya secara manual di terminal mereka sendiri.
